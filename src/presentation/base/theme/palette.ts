export interface Palette {
  primary: string;
  primaryText: string;
  primaryLight: string;
  gradientStart: string;
  gradientEnd: string;
  background: string;
  text: string;
  textMuted: string;
  secondary: string;
  secondaryText: string;
}
