export interface DarkArgs {
  primary: string;
  primaryText: string;
  primaryLight: string;
  gradientStart: string;
  gradientEnd: string;
  background: string;
  secondary: string;
  secondaryText: string;
  text?: string;
  textMuted?: string;
}
