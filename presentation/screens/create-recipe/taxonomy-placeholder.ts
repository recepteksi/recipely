/**
 * Fallback emoji shown for a cuisine/category whose key has no entry in the
 * backend catalog or the local emoji maps (e.g. an unknown/legacy key, or
 * before the taxonomy store has loaded).
 */
export const TAXONOMY_PLACEHOLDER_EMOJI = '🍽️';
